{
  "name": "functions",
  "description": "Firebase Cloud Functions for Project Rebound",
  "scripts": {
    "serve": "firebase emulators:start --only functions,auth --project project-rebound",
    "deploy": "firebase deploy --only functions"
  },
  "engines": {
    "node": "24"
  },
  "main": "index.js",
  "dependencies": {
    "firebase-admin": "^11.0.0",
    "firebase-functions": "^7.2.5"
  },
  "private": true
}
